apiVersion: htc.cern.ch/v1alpha1
kind: HTCJob
metadata:
  name: example-htcjob
spec:
  template:
    spec:
      containers:
      - name: pi
        image: perl
        command: ["perl",  "-Mbignum=bpi", "-wle", "print bpi(2000)"]
      restartPolicy: Never
  backoffLimit: 4
